html(lang='en')
	head
		meta(charset='utf-8' name='viewport' content='width=device-width, initial-scale=1.0')
		title QuikShop
		link(rel='stylesheet', href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css')
		link(rel='stylesheet', href='style.css')
		script(src='scripts.js')
	body(onload = "openDefault()")
		div(class='container')
			div(class='tab')
				img(src='quikshop_logo.png' style='max-width:100%; height: auto;')
				button(id='defaultButton' class='tablinks' onclick="openTab(event, 'welcome')" style='font-size: 1.8vw') Welcome
				button(class='tablinks' onclick="openTab(event, 'blockchain')" style='font-size: 1.8vw') BlockChain
				button(class='tablinks' onclick="openTab(event, 'contact')" style='font-size: 1.8vw') Contact us
		div(id='welcome' class='tabcontent')
			h2(style='font-size:3vw') Welcome to Quickshop!
			p(class='introPara' style='font-size: 1.6vw') This is Group 11's project QuickShop for CPEN391 Module 2. It includes hashing as well as blockchain mining on the DE1-SoC, where the data is sent over to a database in SQL. A server running NodeJs communicates with the database to display the most up to date blockchain. HTML pages are generated dynamically using Pug View.
			img(src='blockchain.png' style='max-width:100%; height: auto;')
			a(href='https://github.com/Mike-Yue/module2' target='_blank') Source Code

		div(id='blockchain' class='tabcontent')
			h2(display='inline' class='specialh2' style='font-size:3vw') Live View of Blockchain
			div(class='reset-search')
				form(action='/reset' method='get')
					button Reset Search
			div(class='search-container')
				form(action='/search' method='get')
					input(class='tablinks' type='text' name='name' placeholder='Search...')
					button Search
			div(class='table-responsive')
				if(blockNum.length)
					table(id='blockchain table' class='table table-bordered')
						thead
							tr
								th='Block'
								for block in blockNum
									th= block
						tbody
							tr
								th='Nonce'
								for nonceVal in nonce
									td= nonceVal
							tr(class='data')
								th='Data'
								for dataVal in data
									td= dataVal
							tr
								th='Previous Hash'
								for pHashVal in prevHash
									td= pHashVal
							tr
								th='Current Hash'
								for hashVal in currHash
									td= hashVal

				else
					p No elements!


		div(id='contact' class='tabcontent')
			h2 Contact Information
			p Align center?
